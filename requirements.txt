# RouteDiff dependencies (CPU only, free tools).
# Install torch from the CPU index:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
#
# Python 3.11 is the tested interpreter.

# Core ML (CPU build)
torch==2.12.1

# Numerics
numpy==2.4.6

# Backend server
fastapi==0.138.1
uvicorn[standard]==0.49.0

# Baseline solver
ortools==9.15.6755

# Offline plotting
matplotlib==3.11.0

# Testing
pytest==9.1.1
httpx==0.28.1  # FastAPI TestClient transport
